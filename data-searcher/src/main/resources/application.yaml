server:
  port: 8082

spring:
  datasource:
    #url: jdbc:postgresql://localhost:5432/data-searcher
    url: jdbc:h2:mem:test
    username: sa
    #password: test
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create